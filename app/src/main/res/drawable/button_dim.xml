<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state (dimmed) -->
    <item android:state_enabled="false">
        <layer-list>
            <item
                android:drawable="@drawable/next_prev_button"
                android:alpha="0.4" />
        </layer-list>
    </item>

    <!-- Default (enabled) -->
    <item android:drawable="@drawable/next_prev_button" />
</selector>
